import Flashcard from "./Flashcard";
import Homepage from "./Homepage";

export default function App(){
    // Read URL variables 

    // if (there are the variables){
    //    return (<Flashcard(...variables) />); 
    // }
    // else {
    //    return (<Homepage />);
    // }

    // temporary
    return (
        <div>App</div>
    );
}